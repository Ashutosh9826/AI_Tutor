# Frontend

React + Vite client for AI Tutor.

## Commands

- `npm run dev`
- `npm run build`
- `npm run lint`

## Environment

Create `.env` from `.env.example`:

- `VITE_API_URL`
- `VITE_GOOGLE_CLIENT_ID`
