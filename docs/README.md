# Docs

- `archive/`: previous summary and implementation checklist snapshots.
- `design_mockups_legacy/`: legacy design artifacts moved out of runtime paths.

These files are kept for reference only and are not source-of-truth for runtime behavior.
